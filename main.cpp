// cpp main file used to invoke kernel calls

#include "main.h"
#include "src/headers/kernel.h"

using namespace std;

int main()
{
	//launchKernel(10, 10);
	//testVector();

	cout << "Hello CMake." << endl;
	return 0;
}
